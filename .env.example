# Fal.ai (for fal_nb2 / fal_nb_pro backends)
FAL_KEY=your-fal-key-here

# Google Vertex AI (default V5 backend = gemini-2.5-flash-image)
# Drop the service account JSON file at the package root as
# `gemini_service_account.json` — no env var needed.
